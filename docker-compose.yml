version: '3.8'

services:
  # PostgreSQL Database
  postgres:
    image: postgres:15-alpine
    container_name: twitter-postgres
    environment:
      POSTGRES_DB: twitter_users
      POSTGRES_USER: twitter_user
      POSTGRES_PASSWORD: twitter_password
      POSTGRES_INITDB_ARGS: "--encoding=UTF-8 --lc-collate=C --lc-ctype=C"
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./sql:/docker-entrypoint-initdb.d
    networks:
      - twitter-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U twitter_user -d twitter_users"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  # Users API Service
  users-api:
    build:
      context: .
      dockerfile: services/users-api/Dockerfile
    container_name: twitter-users-api
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: docker
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/twitter_users
      SPRING_DATASOURCE_USERNAME: twitter_user
      SPRING_DATASOURCE_PASSWORD: twitter_password
      SPRING_DATASOURCE_DRIVER_CLASS_NAME: org.postgresql.Driver
      SPRING_JPA_HIBERNATE_DDL_AUTO: validate
      SPRING_JPA_SHOW_SQL: false
      LOGGING_LEVEL_COM_TWITTER: DEBUG
      LOGGING_LEVEL_ORG_HIBERNATE_SQL: DEBUG
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - twitter-network
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/actuator/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  # Optional: pgAdmin for database management
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: twitter-pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@twitter.com
      PGADMIN_DEFAULT_PASSWORD: admin123
      PGADMIN_CONFIG_SERVER_MODE: 'False'
    ports:
      - "5050:80"
    depends_on:
      - postgres
    networks:
      - twitter-network
    restart: unless-stopped

volumes:
  postgres_data:
    driver: local

networks:
  twitter-network:
    driver: bridge
    ipam:
      config:
        - subnet: 172.20.0.0/16
